/**
 * Vitest 测试环境初始化文件
 * 在运行测试前自动加载，引入 jest-dom 扩展断言方法
 * （如 toBeInTheDocument、toHaveTextContent 等）。
 * 使用 @testing-library/jest-dom/vitest 入口以兼容 vitest。
 */
import "@testing-library/jest-dom/vitest";
