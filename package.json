{
  "_comment": "OierTool 根级 npm workspaces 配置。web/ 和 crawler/ 共享公共开发依赖。",
  "name": "oiertool",
  "description": "算法竞赛选手的随身工具站 - 自动聚合 5 大 OJ 平台竞赛数据",
  "private": true,
  "workspaces": [
    "web",
    "crawler"
  ],
  "scripts": {
    "test": "npm run test -w web && npm run test -w crawler",
    "test:web": "npm run test -w web",
    "test:crawler": "npm run test -w crawler",
    "lint": "npm run lint -w web && npm run lint -w crawler",
    "typecheck": "npm run typecheck -w web && npm run typecheck -w crawler",
    "check": "npm run typecheck && npm run lint && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^20",
    "@types/node-cron": "^3.0.11",
    "@vitejs/plugin-react": "^6.0.2",
    "jsdom": "^29.1.1",
    "prisma": "^7.8.0",
    "dotenv": "^16.4.7",
    "tsx": "^4.0.0",
    "typescript": "^5",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "@next/env": "^16.2.9",
    "@prisma/adapter-pg": "^7.8.0"
  }
}
